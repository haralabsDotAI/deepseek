# yet another innovation
pre-training
n/a
training
n/a
post-training
suprevised reenforcement learning tunning
distillation from older related models

# bootstrap deepseek inference on VS Code
    python3.13 -m pip install --upgrade pip
    python3 -m venv path/to/venv
    source path/to/venv/bin/activate
    python3 -m pip install xyz
# refactor to work on non CUDA/GPU's - Custom Chips(Traninum - amazon,TPU - google)
